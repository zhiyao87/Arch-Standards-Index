# ════════════════════════════════════════════════════════════════════
#  .gitignore — 建筑标准规范索引库
#
#  入库原则：只跟踪「数据源 + 生成器 + 说明书」。
#  由 build_index.py 产出的内容一概不入库，克隆后跑一次即可重建：
#
#      git clone <仓库地址> && cd standards-index
#      python build_index.py
#
#  这样处理的好处：仓库里永远不会出现「源数据改了、产物忘了重新生成」
#  的不一致状态 —— 因为产物根本不在版本控制里。
# ════════════════════════════════════════════════════════════════════


# ── 生成产物 ────────────────────────────────────────────────────────
# Excel 友好版导出（UTF-8 BOM），由 data/standards.csv 生成
/data/standards_excel.csv

# Obsidian 笔记库：主索引页 ＋ 83 个标准笔记
/obsidian/


# ── 入库例外说明 ────────────────────────────────────────────────────
# README.md 同为生成产物，但它是 GitHub 仓库首页（打开即渲染）。
# 保留入库，访客无需运行脚本就能看到完整索引表。
# 若你也希望 README 改为按需生成，取消下面一行的注释：
# /README.md


# ── 私有文档 ────────────────────────────────────────────────────────
# 一份针对本地文件夹的版权分类审计，内容包含个人目录结构与本地文件清单，
# 与本索引库主题无关，也不适合随公开仓库分发。
# 仓库为 public 时请保持排除；确需入库时删除下面一行。
/上海地方文件_版权分类台账.md


# ── 系统垃圾 ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini


# ── Python ──────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/


# ── 编辑器 ──────────────────────────────────────────────────────────
.vscode/
.idea/


# ── Obsidian 配置（笔记内容已由上面的 /obsidian/ 规则整体排除）─────
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/workspace*.json
.trash/


# ── 本地私有内容 ────────────────────────────────────────────────────
private/
*.local.md
